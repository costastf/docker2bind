============
Installation
============

At the command line::

    $ pip install docker2bind

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv docker2bind
    $ pip install docker2bind
